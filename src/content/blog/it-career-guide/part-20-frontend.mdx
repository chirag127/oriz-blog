---
title: "Part 20: Modern Frontend - React, Astro & Full-Stack Patterns"
description: "Learn React hooks, component patterns, Astro Islands, SSR vs SSG, Tailwind CSS, state management, and API integration for modern frontend development."
pubDate: 2026-05-29
tags: ["Frontend", "React", "Astro", "Tailwind CSS", "Career Guide"]
category: "Career Guide"
author: "Chirag Singhal"
series: "The 2026 IT Career Blueprint"
part: 20
draft: false
---

# Part 20: Modern Frontend - React, Astro & Full-Stack Patterns

*[← Back to Master Index](/blog/it-career-guide)*

---

## 1. Why Frontend in 2026?

Frontend engineering is no longer optional for backend developers. Engineers with full-stack skills command **15-25% higher salaries** in platform engineering roles.

### Key Technologies
- **React**: Component-based UI library
- **Astro**: Modern static site generator
- **Tailwind CSS**: Utility-first CSS framework
- **TypeScript**: Type-safe JavaScript

---

## 2. React Fundamentals

### Component Patterns

```jsx
// Functional Component
function UserProfile({ user, onUpdate }) {
  const [editing, setEditing] = useState(false);
  const [localUser, setLocalUser] = useState(user);
  
  const handleSave = () => {
    onUpdate(localUser);
    setEditing(false);
  };
  
  if (editing) {
    return (
      <div>
        <input 
          value={localUser.name}
          onChange={(e) => setLocalUser({...localUser, name: e.target.value})}
        />
        <button onClick={handleSave}>Save</button>
      </div>
    );
  }
  
  return (
    <div>
      <h2>{localUser.name}</h2>
      <button onClick={() => setEditing(true)}>Edit</button>
    </div>
  );
}
```

### Custom Hooks

```jsx
// useLocalStorage hook
function useLocalStorage(key, initialValue) {
  const [storedValue, setStoredValue] = useState(() => {
    try {
      const item = window.localStorage.getItem(key);
      return item ? JSON.parse(item) : initialValue;
    } catch (error) {
      return initialValue;
    }
  });
  
  const setValue = (value) => {
    setStoredValue(value);
    window.localStorage.setItem(key, JSON.stringify(value));
  };
  
  return [storedValue, setValue];
}

// Usage
function App() {
  const [theme, setTheme] = useLocalStorage('theme', 'light');
  // ...
}
```

### Data Fetching

```jsx
// React Query for server state
import { useQuery, useMutation } from '@tanstack/react-query';

function UserList() {
  const { data, isLoading, error } = useQuery({
    queryKey: ['users'],
    queryFn: () => fetch('/api/users').then(res => res.json())
  });
  
  const mutation = useMutation({
    mutationFn: newUser => fetch('/api/users', {
      method: 'POST',
      body: JSON.stringify(newUser)
    }),
    onSuccess: () => queryClient.invalidateQueries(['users'])
  });
  
  if (isLoading) return <div>Loading...</div>;
  if (error) return <div>Error: {error.message}</div>;
  
  return (
    <ul>
      {data.map(user => (
        <li key={user.id}>{user.name}</li>
      ))}
    </ul>
  );
}
```

---

## 3. Astro Framework

### Astro Components

```astro
---
// src/components/Header.astro
interface Props {
  title: string;
  user?: {
    name: string;
    avatar: string;
  };
}

const { title, user } = Astro.props;
---

<header>
  <h1>{title}</h1>
  {user && (
    <img src={user.avatar} alt={user.name} width="40" height="40" />
  )}
</header>
```

### API Routes

```typescript
// src/pages/api/users.ts
import type { APIRoute } from 'astro';

export const GET: APIRoute = async ({ request }) => {
  const response = await fetch('https://api.example.com/users');
  const users = await response.json();
  
  return new Response(JSON.stringify(users), {
    headers: {
      'Content-Type': 'application/json',
    },
  });
};
```

### Islands Architecture

```astro
---
// src/pages/index.astro
import Counter from '../components/Counter.jsx';
---

<html>
  <body>
    <h1>Welcome to Astro</h1>
    <!-- Static content rendered on server -->
    <p>This is static HTML</p>
    
    <!-- Interactive island -->
    <Counter client:load />
  </body>
</html>
```

---

## 4. Tailwind CSS

### Utility-First Classes

```html
<div class="max-w-4xl mx-auto p-4">
  <div class="bg-white rounded-lg shadow-md p-6">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">
      User Profile
    </h2>
    <div class="space-y-4">
      <div class="flex items-center space-x-4">
        <img 
          src="avatar.jpg" 
          alt="User avatar"
          class="w-12 h-12 rounded-full"
        />
        <div>
          <p class="font-medium text-gray-700">John Doe</p>
          <p class="text-sm text-gray-500">john@example.com</p>
        </div>
      </div>
    </div>
  </div>
</div>
```

### Responsive Design

```html
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  <div class="bg-white p-4 rounded-lg shadow">
    <h3 class="font-bold">Card Title</h3>
    <p class="text-gray-600">Card content</p>
  </div>
</div>
```

### Dark Mode

```html
<div class="bg-white dark:bg-gray-800">
  <p class="text-gray-800 dark:text-white">
    Content with dark mode support
  </p>
</div>
```

---

## 5. State Management

### Context API

```jsx
// AuthContext.jsx
const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);
  
  useEffect(() => {
    // Check authentication status
    checkAuth().then setUser(setLoading(false));
  }, []);
  
  const login = async (credentials) => {
    const user = await api.login(credentials);
    setUser(user);
  };
  
  const logout = () => {
    setUser(null);
  };
  
  return (
    <AuthContext.Provider value={{ user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

// Usage
const { user, login, logout } = useContext(AuthContext);
```

### Redux Pattern

```jsx
// store.js
import { configureStore, createSlice } from '@reduxjs/toolkit';

const userSlice = createSlice({
  name: 'user',
  initialState: null,
  reducers: {
    setUser: (state, action) => action.payload,
    clearUser: (state) => null,
  },
});

export const store = configureStore({
  reducer: {
    user: userSlice.reducer,
  },
});

// Component
const UserProfile = () => {
  const user = useSelector(state => state.user);
  const dispatch = useDispatch();
  
  const handleLogout = () => {
    dispatch(clearUser());
  };
};
```

---

## 6. API Integration

### RESTful API Client

```typescript
// api/client.ts
class APIClient {
  private baseURL: string;
  private token?: string;
  
  constructor(baseURL: string) {
    this.baseURL = baseURL;
  }
  
  setToken(token: string) {
    this.token = token;
  }
  
  async request<T>(endpoint: string, options?: RequestInit): Promise<T> {
    const url = `${this.baseURL}${endpoint}`;
    const headers = {
      'Content-Type': 'application/json',
      ...(this.token && { Authorization: `Bearer ${this.token}` }),
      ...options?.headers,
    };
    
    const response = await fetch(url, {
      ...options,
      headers,
    });
    
    if (!response.ok) {
      throw new Error(`API Error: ${response.statusText}`);
    }
    
    return response.json();
  }
  
  get<T>(endpoint: string): Promise<T> {
    return this.request<T>(endpoint);
  }
  
  post<T>(endpoint: string, data: any): Promise<T> {
    return this.request<T>(endpoint, {
      method: 'POST',
      body: JSON.stringify(data),
    });
  }
}

export const api = new APIClient('/api');
```

### GraphQL Client

```typescript
// graphql/client.ts
import { ApolloClient, InMemoryCache, createHttpLink } from '@apollo/client';
import { setContext } from '@apollo/client/link/context';

const httpLink = createHttpLink({
  uri: '/graphql',
});

const authLink = setContext((_, { headers }) => {
  const token = localStorage.getItem('token');
  return {
    headers: {
      ...headers,
      authorization: token ? `Bearer ${token}` : "",
    }
  }
});

export const apolloClient = new ApolloClient({
  link: authLink.concat(httpLink),
  cache: new InMemoryCache(),
});
```

---

## 7. Resource Directory: Frontend

### Best Books

| Book | Author | Price | Key Topics |
|------|--------|-------|------------|
| **React - The Complete Guide** | Maximilian Schwarzmüller | Paid | React mastery |
| **Fullstack React** | Anthony Accomazzo | Paid | React patterns |
| **Learning GraphQL** | O'Reilly | Paid | GraphQL guide |
| **CSS Secrets** | Lea Verou | Paid | CSS techniques |

### Best Udemy Courses

| Course | Instructor | Price (INR) | Key Topics |
|--------|------------|-------------|------------|
| **React - The Complete Guide** | Maximilian Schwarzmüller | ₹3,999-4,999 | React fundamentals |
| **Modern React with Redux** | Stephen Grider | ₹2,999-3,999 | Hooks, Redux |
| **The Complete React Course** | Andrew Mead | ₹2,999-3,999 | React patterns |
| **Astro Masterclass** | Instructor | ₹1,499-2,299 | Astro framework |

### Best O'Reilly Resources

| Resource | Topic | Access |
|----------|-------|--------|
| **Learning React** | O'Reilly | Paid |
| **Fullstack React** | O'Reilly | Paid |
| **GraphQL in Action** | O'Reilly | Paid |

### Best LinkedIn Learning Courses

| Course | Instructor | Access |
|--------|------------|--------|
| **React Essential Training** | Eve Porcello | Paid |
| **Astro Fundamentals** | Instructor | Paid |
| **Tailwind CSS Essential Training** | Barron Stone | Paid |

### Free Resources

| Platform | Resource | Link |
|----------|----------|------|
| **React Docs** | Official docs | react.dev |
| **Astro Docs** | Official docs | docs.astro.build |
| **Tailwind Docs** | Official docs | tailwindcss.com/docs |
| **Awesome React** | GitHub | github.com/enaqx/awesome-react |

---

## 8. Common Frontend Interview Questions

| Question | Answer |
|----------|--------|
| **Difference between SSR and SSG?** | SSR renders on server per request, SSG pre-renders at build time. |
| **What are React Hooks?** | Functions that let you use state and other React features in functional components. |
| **How to optimize React performance?** | Memo, useCallback, lazy loading, code splitting. |
| **What is the virtual DOM?** | Lightweight copy of actual DOM for efficient updates. |
| **How to handle forms in React?** | Controlled components with state, or uncontrolled with refs. |

---

## 9. Part Navigation

### Previous Parts
[Part 19: CI/CD Pipelines](/blog/it-career-guide/part-19-cicd)

### Next Parts
[Part 21: Generative AI Fundamentals](/blog/it-career-guide/part-21-genai-fundamentals) ·
[Part 22: Vector Databases](/blog/it-career-guide/part-22-vector-databases)

---

*[Proceed to Part 21: Generative AI Fundamentals →](/blog/it-career-guide/part-21-genai-fundamentals)*