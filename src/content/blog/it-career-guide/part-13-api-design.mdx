---
title: "Part 13: API Design Patterns & GraphQL"
description: "Learn RESTful best practices, OpenAPI specification, rate limiting, pagination, GraphQL schema design, Apollo Server, and real-time subscriptions."
pubDate: 2026-05-29
tags: ["API Design", "GraphQL", "Backend", "Career Guide"]
category: "Career Guide"
author: "Chirag Singhal"
series: "The 2026 IT Career Blueprint"
part: 13
draft: false
---

# Part 13: API Design Patterns & GraphQL

*[← Back to Master Index](/blog/it-career-guide)*

---

## 1. RESTful API Design Principles

### HTTP Status Codes

| Code | Meaning | Use Case |
|------|---------|----------|
| **200** | OK | Successful GET, PUT, PATCH |
| **201** | Created | Resource created via POST |
| **204** | No Content | Successful DELETE |
| **400** | Bad Request | Invalid request body |
| **401** | Unauthorized | Authentication required |
| **403** | Forbidden | Insufficient permissions |
| **404** | Not Found | Resource doesn't exist |
| **429** | Too Many Requests | Rate limiting |
| **500** | Internal Server Error | Server-side errors |

### REST API Design Patterns

```python
from fastapi import FastAPI, status, HTTPException
from pydantic import BaseModel

app = FastAPI()

class UserCreate(BaseModel):
    name: str
    email: str
    age: int

# POST - Create resource
@app.post("/users/", status_code=status.HTTP_201_CREATED)
async def create_user(user: UserCreate):
    # Validate input
    if user.age < 0:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="Age must be positive"
        )
    return {"id": 1, **user.model_dump()}

# GET - Retrieve resource
@app.get("/users/{user_id}", status_code=status.HTTP_200_OK)
async def get_user(user_id: int):
    return {"id": user_id, "name": "John Doe"}

# PUT - Update resource
@app.put("/users/{user_id}", status_code=status.HTTP_200_OK)
async def update_user(user_id: int, user: UserCreate):
    return {"id": user_id, **user.model_dump()}

# DELETE - Remove resource
@app.delete("/users/{user_id}", status_code=status.HTTP_204_NO_CONTENT)
async def delete_user(user_id: int):
    return None
```

---

## 2. OpenAPI Specification

### OpenAPI 3.0 Example

```yaml
openapi: 3.0.0
info:
  title: User API
  version: 1.0.0
  description: API for user management

servers:
  - url: https://api.example.com/v1
    description: Production server

paths:
  /users:
    get:
      summary: List all users
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: List of users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'

components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
          format: email
```

### FastAPI Auto-Documentation

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI(
    title="User API",
    description="API for user management",
    version="1.0.0"
)

class User(BaseModel):
    id: int
    name: str
    email: str

@app.get("/users/", response_model=list[User])
async def list_users():
    """Retrieve all users with pagination"""
    return []
```

---

## 3. Rate Limiting

### Token Bucket Algorithm

```python
import time
import asyncio
from collections import defaultdict

class RateLimiter:
    def __init__(self, rate: int, per: int):
        self.rate = rate
        self.per = per
        self.clients = defaultdict(list)
    
    async def is_allowed(self, client_id: str) -> bool:
        now = time.time()
        window_start = now - self.per
        
        # Remove old requests
        self.clients[client_id] = [
            req_time for req_time in self.clients[client_id]
            if req_time > window_start
        ]
        
        if len(self.clients[client_id]) < self.rate:
            self.clients[client_id].append(now)
            return True
        return False

rate_limiter = RateLimiter(rate=100, per=60)  # 100 requests per minute

@app.middleware("http")
async def rate_limit_middleware(request: Request, call_next):
    client_id = request.client.host
    if not await rate_limiter.is_allowed(client_id):
        return JSONResponse(
            status_code=429,
            content={"error": "Rate limit exceeded"}
        )
    return await call_next(request)
```

### Sliding Window Log

```python
import redis
import time

class SlidingWindowRateLimiter:
    def __init__(self, redis_client, key_prefix: str, limit: int, window: int):
        self.redis = redis_client
        self.key_prefix = key_prefix
        self.limit = limit
        self.window = window
    
    def is_allowed(self, key: str) -> bool:
        full_key = f"{self.key_prefix}:{key}"
        now = time.time()
        window_start = now - self.window
        
        # Remove old entries
        self.redis.zremrangebyscore(full_key, 0, window_start)
        
        # Count current entries
        count = self.redis.zcard(full_key)
        
        if count < self.limit:
            self.redis.zadd(full_key, {str(now): now})
            self.redis.expire(full_key, self.window)
            return True
        return False
```

---

## 4. Pagination Patterns

### Cursor-Based Pagination

```python
from fastapi import FastAPI, Query

app = FastAPI()

@app.get("/users/")
async def list_users(
    cursor: str | None = Query(None),
    limit: int = Query(10, le=100)
):
    query = "SELECT * FROM users"
    params = []
    
    if cursor:
        query += " WHERE id > %s"
        params.append(cursor)
    
    query += " ORDER BY id LIMIT %s"
    params.append(limit)
    
    # Execute query
    users = await execute_query(query, params)
    
    # Generate next cursor
    next_cursor = users[-1]['id'] if users else None
    
    return {
        "data": users,
        "next_cursor": next_cursor
    }
```

### Offset-Based Pagination

```python
@app.get("/products/")
async def list_products(page: int = Query(1, ge=1), per_page: int = Query(10, le=100)):
    offset = (page - 1) * per_page
    
    query = f"SELECT * FROM products LIMIT {per_page} OFFSET {offset}"
    products = await execute_query(query)
    
    total = await get_total_products()
    total_pages = (total + per_page - 1) // per_page
    
    return {
        "data": products,
        "meta": {
            "page": page,
            "per_page": per_page,
            "total": total,
            "total_pages": total_pages
        }
    }
```

---

## 5. GraphQL Fundamentals

### Schema Design

```graphql
type User {
  id: ID!
  name: String!
  email: String!
  posts: [Post!]!
  createdAt: String!
}

type Post {
  id: ID!
  title: String!
  content: String!
  author: User!
  publishedAt: String
}

type Query {
  users(limit: Int, offset: Int): [User!]!
  user(id: ID!): User
  posts(authorId: ID!, published: Boolean): [Post!]!
}

type Mutation {
  createUser(input: CreateUserInput!): User!
  createPost(input: CreatePostInput!): Post!
}

input CreateUserInput {
  name: String!
  email: String!
}

input CreatePostInput {
  title: String!
  content: String!
  authorId: ID!
}
```

### Resolvers

```python
from ariadne import QueryType, MutationType, make_executable_schema
import strawberry

@strawberry.type
class User:
    id: int
    name: str
    email: str

@strawberry.type
class Query:
    @strawberry.field
    def users(self, limit: int = 10, offset: int = 0) -> list[User]:
        return get_users(limit, offset)
    
    @strawberry.field
    def user(self, id: int) -> User:
        return get_user(id)

@strawberry.type
class Mutation:
    @strawberry.mutation
    def create_user(self, name: str, email: str) -> User:
        return create_new_user(name, email)

schema = strawberry.Schema(query=Query, mutation=Mutation)
```

---

## 6. GraphQL vs REST

### When to Use GraphQL

| Scenario | GraphQL | REST |
|----------|---------|------|
| **Complex data requirements** | ✅ Single endpoint, flexible queries | ❌ Multiple endpoints needed |
| **Mobile apps** | ✅ Reduced payload size | ❌ Over-fetching common |
| **Rapid frontend iteration** | ✅ Schema evolves independently | ❌ Backend changes required |
| **Simple APIs** | ❌ Overhead of query parsing | ✅ Straightforward |

### Performance Comparison

```javascript
// REST - Multiple requests
GET /users/1
GET /users/1/posts
GET /users/1/friends

// GraphQL - Single request
query {
  user(id: 1) {
    name
    posts {
      title
    }
    friends {
      name
    }
  }
}
```

---

## 7. API Security

### JWT Authentication

```python
from fastapi import Depends, HTTPBearer, HTTPAuthorizationCredentials
import jwt

security = HTTPBearer()

async def verify_token(credentials: HTTPAuthorizationCredentials = Depends(security)):
    try:
        payload = jwt.decode(
            credentials.credentials,
            SECRET_KEY,
            algorithms=["HS256"]
        )
        return payload
    except jwt.ExpiredSignatureError:
        raise HTTPException(status_code=401, detail="Token expired")
    except jwt.InvalidTokenError:
        raise HTTPException(status_code=401, detail="Invalid token")

@app.get("/protected")
async def protected_route(user: dict = Depends(verify_token)):
    return {"message": f"Hello {user['sub']}"}
```

### Input Validation

```python
from pydantic import BaseModel, EmailStr, field_validator

class CreateUser(BaseModel):
    name: str
    email: EmailStr
    age: int
    password: str
    
    @field_validator('age')
    @classmethod
    def validate_age(cls, v):
        if v < 13:
            raise ValueError('Must be at least 13 years old')
        return v
    
    @field_validator('password')
    @classmethod
    def validate_password(cls, v):
        if len(v) < 8:
            raise ValueError('Password must be at least 8 characters')
        return v
```

---

## 8. Resource Directory: API Design

### Best Books

| Book | Author | Price | Key Topics |
|------|--------|-------|------------|
| **RESTful Web APIs** | Leonard Richardson | Paid | REST design |
| **GraphQL: The Definitive Guide** | Reed Davis | Paid | GraphQL mastery |
| **API Design Patterns** | JJ Geewax | Paid | Advanced patterns |
| **Web APIs: HTTP and REST** | Jim Wilcox | Paid | HTTP fundamentals |

### Best Udemy Courses

| Course | Instructor | Price (INR) | Key Topics |
|--------|------------|-------------|------------|
| **REST API Development** | Colt Steele | ₹999-1,499 | REST patterns |
| **GraphQL & Apollo** | Andrew Mead | ₹999-1,499 | GraphQL full course |
| **API Design & Testing** | Jose Portilla | ₹999-1,499 | Design & testing |
| **Building APIs with GraphQL** | Sarah Drasner | ₹799-1,199 | GraphQL advanced |

### Best O'Reilly Resources

| Resource | Topic | Access |
|----------|-------|--------|
| **RESTful Web APIs** | O'Reilly | Paid |
| **GraphQL in Action** | O'Reilly | Paid |
| **API Design Patterns** | O'Reilly | Paid |

### Best LinkedIn Learning Courses

| Course | Instructor | Access |
|--------|------------|--------|
| **RESTful API Design** | Keith Cunningham | Paid |
| **GraphQL Essential Training** | Eve Porcello | Paid |
| **API Testing** | Barron Stone | Paid |

### Free Resources

| Platform | Resource | Link |
|----------|----------|------|
| **GraphQL Docs** | Official docs | graphql.org/learn |
| **REST API Tutorial** | Codecademy | codecademy.com/learn/rest-api |
| **Awesome API** | GitHub | github.com/ConditionalSucks/awesome-api |
| **API Design Guidelines** | Google | cloud.google.com/apis-design |

---

## 9. Common API Interview Questions

| Question | Answer |
|----------|--------|
| **How to handle API versioning?** | URL versioning, header versioning, or query parameter versioning. |
| **What is rate limiting?** | Limit API calls per user/IP to prevent abuse and ensure fair usage. |
| **How to handle pagination?** | Cursor-based or offset-based pagination with metadata. |
| **What are GraphQL advantages?** | Single endpoint, no over-fetching, strong typing, real-time subscriptions. |
| **How to secure APIs?** | JWT, OAuth, input validation, rate limiting, CORS. |

---

## 10. Part Navigation

### Previous Parts
[Part 12: Microservices Architecture](/blog/it-career-guide/part-12-microservices)

### Next Parts
[Part 14: Docker Containers](/blog/it-career-guide/part-14-docker) ·
[Part 15: Kubernetes Orchestration](/blog/it-career-guide/part-15-kubernetes)

---

*[Proceed to Part 14: Docker Containers →](/blog/it-career-guide/part-14-docker)*